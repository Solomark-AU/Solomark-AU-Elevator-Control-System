#include "RequestAssignment.hpp"
#include "ElevatorDispatching.hpp"



int main()
{
    init_module("configure.json");
    while (1)
    {
        int req, tar;
        cin >> req >> tar;
        cout<<"F**k zy\n";
        add_target(req, tar);
        cout<<"F**k RBQ\n";
    }
    return 0;
}
